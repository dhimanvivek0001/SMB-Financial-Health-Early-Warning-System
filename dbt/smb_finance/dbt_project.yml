name: 'smb_finance'
version: '1.0.0'
config-version: 2

profile: 'smb_finance'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  smb_finance:
    staging:
      +materialized: view
      +schema: analytics
    intermediate:
      +materialized: table
      +schema: analytics
    marts:
      +materialized: table
      +schema: analytics
